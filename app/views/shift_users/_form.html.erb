<%= form_with(model: shift_user, local: true) do |form| %>
  <% if shift_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shift_user.errors.count, "error") %> prohibited this shift_user from being saved:</h2>

      <ul>
      <% shift_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :shift_id %>
    <%= form.number_field :shift_id %>
  </div>

  <div class="field">
    <%= form.label :work_type %>
    <%= form.number_field :work_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
